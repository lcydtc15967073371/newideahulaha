# 开发教训：虚拟定位APP踩坑记录

> ⚠️ **核心教训：不要闭门造车！先查资料再动手！**

## 踩坑经过

### ❌ 坑1：胡乱猜测配置
- 判断 `testOnly="true"` 能出现在开发者列表里 → 安装报 -15
- 然后去掉所有特殊配置 → 列表里看不到应用
- **事实**：正确做法是普通应用 + 声明 `ACCESS_MOCK_LOCATION`（加tools:ignore）

### ❌ 坑2：定位只发3次就停
- 写了for循环只推送3次坐标 → 应用读不到位置
- **事实**：需要**持续推送**（协程每200ms发一次），直到用户停止

### ❌ 坑3：只覆盖gps单一来源
- 只注册了 `gps` provider → 部分APP读不到
- **事实**：需要同时覆盖 `gps`、`network`、`passive` 三个定位源

## 正确做法总结

1. **先搜索！** 花5分钟查资料，省2小时踩坑
2. Android模拟定位 APP：
   - 正常打包（不加 testOnly）
   - 声明 `ACCESS_MOCK_LOCATION` + `tools:ignore="ProtectedPermissions"`
   - 用协程持续推送位置（不要只发几次）
   - 同时覆盖多个定位源（gps/network/passive）
3. 用户操作：开发者选项选择该应用 → 打开APP启动
